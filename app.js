

console.log(Vue)

const app = Vue.createApp({
    // template: `
    // <h1>Hola mundo</h1>
    // <p>Desde app.js</p>
    // <p> {{ true ? 'Activo' : 'Inactivo' }} </p>
    // `
})

app.mount('#myApp')